# Local HTTP server without authentication.
# Use this only when binding to localhost.

export CONTAINER_RUNTIME_MCP_TRANSPORT=http
export CONTAINER_RUNTIME_MCP_HTTP_ADDR=127.0.0.1:8080
export CONTAINER_RUNTIME_MCP_HTTP_PATH=/mcp
