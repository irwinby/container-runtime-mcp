# Read-only HTTP server. Safe to expose without authentication.
# Only read-only tools are registered.

export CONTAINER_RUNTIME_MCP_TRANSPORT=http
export CONTAINER_RUNTIME_MCP_HTTP_ADDR=0.0.0.0:8080
export CONTAINER_RUNTIME_MCP_HTTP_PATH=/mcp
export CONTAINER_RUNTIME_MCP_READ_ONLY=true
